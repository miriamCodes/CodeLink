import Image from 'next/image'
// might want to make it a directory called profile and then a [slug].tsx as dynamic routing as we dont know how many profiles there are going to be
// const router = useRouter();
// const { slug } = router.query;

export default function Profile() {
  return ( )
}
